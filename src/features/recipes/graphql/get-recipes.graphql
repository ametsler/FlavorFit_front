query GetRecipes($input: RecipeFilterInput!) {
  recipes(input: $input) {
    hasMore
    total
    items {
      title
      description
      image
      slug
      cuisineType
      difficulty
      prepTime
      cookTime
      calories
      rating
      likes
      views
      hasLike
    }
  }
}
